import Navigation from './Navigation';

const Header = () => {
    return (
        <header>
            <h1>Ruby Hill</h1>
            <Navigation />
        </header>
    );
};

export default Header;
